node_modules/
dist/
build/
*.log
.DS_Store
.env
.env.*
coverage/
*.tsbuildinfo
# Per-task scratch space: everything an agent does while working lives in the task
# folder, but only readme.md and evidence/ are history — work/ is the future's /tmp.
.eforest/tasks/**/work/
packages/server/work/
# Replay recordings live in the Replay cloud, cited by URL — never committed.
# MP4 walkthrough videos + latest.json land here too (slack-clone convention):
# local proof embedded in report messages, not history.
recordings/

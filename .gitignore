node_modules/
dist/
build/
*.log
.DS_Store
.env
.env.*
coverage/
*.tsbuildinfo
# Per-task scratch space: everything an agent does while working lives in the task
# folder, but only readme.md and evidence/ are history — work/ is the future's /tmp.
.eforest/tasks/**/work/
packages/server/work/
# Legacy local recording artifacts are not browser evidence.
recordings/
# Commit only the Replay QA project binding; ignore local tunnel/session state.
.replay/*
!.replay/config.json
.playwright-cli/
.pnpm-store/

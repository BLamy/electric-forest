E2-T08 no-database sweep
base=e23d04f5754dc3cb74d12ffcdd05fa1a774b07f6 (E2-T07 verified head)
allowed write targets: the E0-T07 stream store (via the Durable Streams server), evidence/, gitignored test scratch

== new dependencies (each package.json in the diff) ==
waived apps/web/package.json: new dependency @eforest/browser-verify — standing E3 browser proof harness; E3-T02 serves static browser code and introduces no storage engine
waived apps/web/package.json: new dependency @eforest/client — E3 browser shell reads official identity stream state; the client package is stream transport, not a storage engine
waived apps/web/package.json: new dependency @eforest/platform — E3 shell type and test access to the existing platform; E3-T02 serves static browser code and introduces no storage engine
waived apps/web/package.json: new dependency @eforest/protocol — out-of-band digest equality in the E3 shell proof; E3-T02 serves static browser code and introduces no storage engine
waived apps/web/package.json: new dependency @eforest/reducers — E3 browser shell uses the existing reducer library for derived stream projections; it introduces no database
waived apps/web/package.json: new dependency @eforest/streamfs — E3 browser file views consume StreamFS event projections; the package is the stream-backed filesystem, not a database
waived apps/web/package.json: new dependency @eforest/web-hooks — E3 browser interactions use the existing typed web hooks; they dispatch through official streams and add no persistence layer
waived apps/web/package.json: new dependency @types/react — compile-time React declarations; E3-T02 serves static browser code and introduces no storage engine
waived apps/web/package.json: new dependency @types/react-dom — compile-time React DOM declarations; E3-T02 serves static browser code and introduces no storage engine
waived apps/web/package.json: new dependency react — E3 in-browser view runtime; E3-T02 serves static browser code and introduces no storage engine
waived apps/web/package.json: new dependency react-dom — E3 DOM renderer; E3-T02 serves static browser code and introduces no storage engine
waived apps/web/package.json: new dependency vite — E3 static bundle compiler; E3-T02 serves static browser code and introduces no storage engine
waived package.json: new dependency replayio — Replay evidence CLI used by the browser harness; it uploads recordings and is not application storage
waived packages/browser-verify/package.json: new dependency @eforest/client — reads official identity stream state out of band; the E3-T02 harness uses fresh tmpdir data and adds no database
waived packages/browser-verify/package.json: new dependency @eforest/identity — compares against the existing identity reducer; the E3-T02 harness uses fresh tmpdir data and adds no database
waived packages/browser-verify/package.json: new dependency @eforest/platform — boots the existing platform in an isolated test world; the E3-T02 harness uses fresh tmpdir data and adds no database
waived packages/browser-verify/package.json: new dependency @eforest/protocol — compares canonical stream digests; the E3-T02 harness uses fresh tmpdir data and adds no database
waived packages/browser-verify/package.json: new dependency @eforest/server — boots the published-reference stream server as test-only infrastructure; the E3-T02 harness uses fresh tmpdir data and adds no database
waived packages/browser-verify/package.json: new dependency playwright-core — drives Replay Chromium for browser verification; the E3-T02 harness uses fresh tmpdir data and adds no database
waived packages/cli/package.json: new dependency @eforest/platform — ef registry rebuild consumes the projector/reducer library; streams-only code, not a storage engine
waived packages/cli/package.json: new dependency @eforest/reducers — ef replay/materialize uses the existing reducer library to rebuild stream state; it adds no database
waived packages/cli/package.json: promoted dependency @eforest/server — ef registry rebuild opens the stream-store data dir through Electric's reference Durable Streams server — the store's own surface IS the E0-T07 stream store (promoted from devDependencies)
waived packages/platform/package.json: new dependency @eforest/reducers — the platform composes the existing reducer library over official streams; it adds no database
waived packages/platform/package.json: new dependency @eforest/streamfs — the platform exposes the existing stream-backed filesystem reducer; it adds no database
waived packages/reducers/package.json: new dependency @eforest/protocol — reducers consume canonical protocol envelopes and offsets; this is stream protocol code, not storage
waived packages/reducers/package.json: new dependency @eforest/streamfs — reducers compose StreamFS event state over official streams; they add no database
waived packages/web-hooks/package.json: new dependency @eforest/protocol — web hooks type and validate canonical stream events; they add no persistence layer
waived packages/web-hooks/package.json: new dependency @eforest/reducers — web hooks call the existing reducer projections for browser state; they add no database
waived packages/web-hooks/package.json: new dependency @types/react — compile-time React declarations for browser hooks; no runtime storage
waived packages/web-hooks/package.json: new dependency react — React runtime for browser hooks; no persistence layer
waived packages/workspace/package.json: new dependency @eforest/protocol — the .ef workspace ledger uses canonical protocol types; it writes caller-owned workspace metadata, not a database

== storage & write tells in changed files ==
allowed .agents/skills/replayio/scripts/browser-open.js: 5 tell(s) — Replay Chromium lifecycle helper (writes caller-selected recording/session state, not application storage)
allowed .eforest/tasks/epic-2-the-gates/E2-T08-registry-derived-index/evidence/e2-t08-cold-clone.txt: 1 tell(s) — committed evidence
allowed .eforest/tasks/epic-2-the-gates/E2-T08-registry-derived-index/readme.md: 7 tell(s) — documentation
allowed .eforest/tasks/epic-4-the-roots/E4-T01-worktree-digest-and-ef-format/readme.md: 1 tell(s) — documentation
allowed apps/web/test/branch-switcher.pw.ts: 5 tell(s) — E3 browser verification harness (writes committed task evidence and gitignored task work)
allowed apps/web/test/file-tree.pw.ts: 5 tell(s) — E3 browser verification harness (writes committed task evidence and gitignored task work)
allowed apps/web/test/file-viewer.pw.ts: 5 tell(s) — E3 browser verification harness (writes committed task evidence and gitignored task work)
allowed apps/web/test/history-event-log.pw.ts: 5 tell(s) — E3 browser verification harness (writes committed task evidence and gitignored task work)
allowed apps/web/test/reading-room-capstone.pw.ts: 5 tell(s) — E3 browser verification harness (writes committed task evidence and gitignored task work)
allowed apps/web/test/registry-live.pw.ts: 5 tell(s) — E3 browser verification harness (writes committed task evidence and gitignored task work)
allowed apps/web/test/repo-home.pw.ts: 5 tell(s) — E3 browser verification harness (writes committed task evidence and gitignored task work)
allowed apps/web/test/shell.pw.ts: 11 tell(s) — E3-T02 browser verify harness (writes only committed task evidence and gitignored task work)
allowed apps/web/test/stream-reducer.pw.ts: 5 tell(s) — E3 browser verification harness (writes committed task evidence and gitignored task work)
allowed packages/cli/src/materialize-command.ts: 4 tell(s) — ef materializer (writes the caller-selected working tree after digest and symlink checks; no database)
allowed packages/cli/src/replay-command.ts: 2 tell(s) — ef replay log emitter (writes only the caller-selected event-log output; no database)
allowed packages/cli/src/worktree.test.ts: 39 tell(s) — test scratch (mkdtemp under tmpdir, removed)
allowed packages/cli/test/registry-command.test.ts: 3 tell(s) — test scratch (mkdtemp under tmpdir, removed)
allowed packages/identity/scripts/verify-provenance-refresh-sensitivity.mjs: 26 tell(s) — provenance sensitivity harness (mutates disposable fixture copies under os.tmpdir(), then removes them)
allowed packages/identity/scripts/verify-provenance-refresh.mjs: 3 tell(s) — identity provenance harness (writes the two E1-T11 evidence files only under its explicit --refresh-approved-e2 flag; flagless runs are read-only)
allowed packages/identity/scripts/verify-work-queue-policy.mjs: 48 tell(s) — work-queue policy self-check harness (every write goes to mkdtempSync scratch under os.tmpdir(), unconditionally — no flag — and is removed in finally; no repo or store writes)
allowed packages/identity/test/identity.test.ts: 3 tell(s) — test scratch (mkdtemp under tmpdir, removed)
allowed packages/platform/README.md: 1 tell(s) — documentation
allowed packages/platform/test/registry.rebuild.test.ts: 9 tell(s) — test scratch (mkdtemp under tmpdir, removed)
allowed packages/platform/test/spa.test.ts: 3 tell(s) — test scratch (mkdtemp under tmpdir, removed)
allowed packages/streamfs/README.md: 1 tell(s) — documentation
allowed packages/streamfs/src/fs.ts: 1 tell(s) — StreamFS event writer (writes content and mutation events through official Durable Streams; no local database)
allowed packages/streamfs/src/worktree.test.ts: 26 tell(s) — test scratch (mkdtemp under tmpdir, removed)
allowed packages/streamfs/test/three-way-merge.integration.test.ts: 14 tell(s) — test scratch (mkdtemp under tmpdir, removed)
allowed packages/workspace/src/index.ts: 9 tell(s) — .ef workspace writer (atomically writes caller-owned workspace metadata; no database)
allowed packages/workspace/src/workspace.test.ts: 7 tell(s) — test scratch (mkdtemp under tmpdir, removed)
allowed tools/replay/e3_t02_final_telemetry.js: 1 tell(s) — Replay evidence lifecycle helper (writes caller-selected journals and receipts, not application storage)
allowed tools/replay/e3_t02_recorder_lifecycle.mjs: 7 tell(s) — Replay evidence lifecycle helper (writes caller-selected journals and receipts, not application storage)
allowed tools/replay/e3_t02_recording_id.mjs: 2 tell(s) — Replay evidence lifecycle helper (writes caller-selected journals and receipts, not application storage)
allowed tools/replay/e3_t02_walkthrough.js: 2 tell(s) — Replay evidence lifecycle helper (writes caller-selected journals and receipts, not application storage)
allowed tools/replay/e3_t02_world.mjs: 3 tell(s) — E3-T02 Replay harness (writes only gitignored task-work truth metadata)
allowed tools/verify/canopy_sensitivity_spine_sabotage.mjs: 2 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/canopy_verify.mjs: 6 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t03_gateway.mjs: 6 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t05_transcript.mjs: 4 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t06_no_database.mjs: 6 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t07_matrix.mjs: 2 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t08_crash.mjs: 2 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t08_destruction.mjs: 6 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t08_evidence.mjs: 4 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t08_lib.mjs: 2 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t08_live.mjs: 2 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t08_matrix.mjs: 2 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t08_no_database.mjs: 6 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t08_no_database_sensitivity.mjs: 1 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t08_refusals.mjs: 2 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t09_sensitivity.mjs: 2 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t10_authz.mjs: 2 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t10_operations.mjs: 2 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t10_sensitivity.mjs: 5 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t11_evidence.mjs: 2 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t11_sensitivity.mjs: 7 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t12_capstone.pw.ts: 2 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t12_portability.mjs: 2 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e2_t12_server.mjs: 2 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e3_t02_playwright_expression.mjs: 1 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e3_t02_production.mjs: 2 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e3_t02_recorder_sensitivity.mjs: 52 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e3_t02_replay_cli_contract.mjs: 12 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e3_t02_wire_sensitivity.mjs: 2 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/e4_t01_evidence.mjs: 7 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/fixtures/e3_t02_fake_replayio.mjs: 7 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/seed-canopy.ts: 12 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)
allowed tools/verify/seed_sensitivity.sh: 2 tell(s) — verify harness (writes evidence/ + mkdtemp scratch)

== restart proof cross-check ==
restart proof: kill -9 + stream-store-copy door equality recorded in e2-t08-destruction.txt (doors-identical=true)

violations=0
E2_T08_NO_DATABASE_OK

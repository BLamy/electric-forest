# E5-T01 real Durable Stream integration

Command:

CI=true pnpm exec vitest run --maxWorkers=1 packages/platform/test/issues.test.ts

The focused suite includes a real `createDurableStreamTestServer` and
`OfficialStreamAdapter` run. It dispatches `issue.opened` and `issue.labeled`
through `PlatformGateway`, bootstraps the resulting durable stream, reduces it
through the registered `issue` definition, and sends an `issue.opened` action to
an `fs:` stream. The latter returns the frozen `unknown-action-type` 404 and
leaves that stream empty.

Observed result:

Test Files 1 passed (1)
Tests 13 passed (13)
E5_T01_REAL_STREAM_INTEGRATION_OK records=2 checkpoint=0000000000000000_0000000000000001 digest=aa2705cdef253a9a41747a50da43ef9a214b415c5740e6ac87847ac48149f423 cross-type=404 untouched=0

The digest line is recorded from the deterministic test assertion; the full
composed and pristine cold-clone targets remain the acceptance gates for this
rework head.

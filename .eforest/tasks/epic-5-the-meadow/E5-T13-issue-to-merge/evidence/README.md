# E5-T13 evidence

The prior static-fixture transcript was invalidated and removed. This directory now holds
the focused verified run: a fresh two-identity browser artifact and screenshots, seven
captured stream dumps and checksums, the frozen E5-T12 replay result, DOM/replay digest
comparison, database-boundary audit, causal timeline, sensitivity report, and clean final
transcript.

Replay upload was unavailable, so `e5-t13-browser.json` plus the deterministic stream
artifacts are the declared fallback. The Replay-shaped URL carried inside the demo's
`evidence.linked` event is a fixture placeholder used to exercise reference rendering; it
is not an uploaded recording or part of the verification claim.
